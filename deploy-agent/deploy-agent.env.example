# GitHub repository to monitor
GITHUB_OWNER=your-github-username-or-org
GITHUB_REPO=palette

# Optional GitHub token (recommended for private repos and higher API limits)
# GITHUB_TOKEN=ghp_xxx

# Install/deploy paths
INSTALL_ROOT=/opt/palette
RELEASES_DIR=/opt/palette/releases
CURRENT_LINK=/opt/palette/current
STATE_DIR=/var/lib/palette-deploy-agent

# Binary/service settings
BINARY_NAME=palette
SERVICE_NAME=palette-app.service

# Path to app environment file used by systemd service
APP_ENV_FILE=/etc/palette/palette.env

# Optional override if your release asset name differs from:
# palette-<version>-linux-<arch>.tar.gz
# ASSET_NAME=palette-1.2.3-linux-amd64.tar.gz

# Optional override if your checksum asset name differs from:
# <asset_name_without_tar_gz>.sha256
# CHECKSUM_NAME=palette-1.2.3-linux-amd64.sha256
